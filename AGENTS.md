# このリポジトリでの作業ルール（Kindle 書籍）

ひな形（kindle-layout-templates）および、そこからコピーした **1冊プロジェクト** 向け。  
書籍制作は `book/`（または `book/JP/`・`book/EN/`）を作業場とし、進行管理は **タスク表（`TASKS.md`）** を正本とする。

## 作業ディレクトリの形

| 形 | いつ |
| --- | --- |
| `book/` 1本 | 1言語だけ（既定・シンプル） |
| `book/JP/` + `book/EN/` | 日本語と英語など複数版 |
| `docs/JP/` + `docs/EN/` | KDP 入力・表紙依頼など出版用（任意） |

- タスク表の場所: `book/TASKS.md` または版ごと `book/JP/TASKS.md` / `book/EN/TASKS.md`  
- 作業中の版のタスク表を読む。両版があるとき章立ては揃える  

## タスク表を必ず確認する

作業を始める前に、毎回その版の `TASKS.md` を読む。

1. いま **完了済み** の項目と **未着手** の項目を把握する  
2. ユーザーが指定した作業が、タスク表上の **次に進めてよい位置** か判断する  
3. 作業後はタスク表の状態（✅ / 🚧 / 🔲）とメモを更新する  

`kindle-book` スキルのフェーズ（章題 → 骨格 → 肉付け → 仕上げ）と矛盾しないよう、タスク表の並びを優先する。

## 先行作業の禁止と指摘

タスク表が完了していない段階で、**先の章・先の工程の本格追加**をしようとした場合は、実行前に指摘する。

例:

- はじめにが未完なのに第5章だけ全文を書く  
- 骨格 HTML が無いのに奥付の仕上げだけ進める  
- 未着手の章を飛ばして付録だけ厚くする  

対応:

1. **いま未完了の前提タスク**をタスク表から列挙する  
2. 先に進めると手戻りが大きい理由を短く述べる  
3. 推奨する次の一手（タスク表の次の 🔲）を示す  
4. ユーザーが **明示的に「飛ばしてよい／例外で進めて」** と言った場合のみ先行を許可し、タスク表にその旨をメモする  

ユーザーが「第○章を書いて」と指定した場合でも、**前提の章が全部 ✅ である必要はない**（章の肉付けは指定順でよい）。ただし次は指摘する:

- タスク表に **その章が存在しない**  
- **骨格・章立てが未確定**のまま本文を増やそうとしている  
- **章題変更と TASKS / 目次 / 章扉の不一致**を直さずに本文だけ足そうとしている  

## 章題が変わったときの生合成

章題・書名・章の増減・順序変更が合意されたら、**同じターンで**次を揃える。HTML だけ先に直してタスク表を残さない。

| 更新先 | やること |
| --- | --- |
| `TASKS.md` | 章名・並び・サマリを新目次に合わせる |
| `with-text.html` | 表紙・目次・章扉・柱（running-head）・次章導線 |
| 他言語版がある場合 | 章立てを同じターンで揃える（文言は各言語） |
| セッションの todo（使う場合） | 章題ラベルを同期 |

## 本文の字下げ（必須）

日本語の本文段落は **先頭を1文字分あける（字下げ）**。

- CSS: `.body-prose p { text-indent: 1em; }`（`with-text.css` に定義済み。消さない）  
- 新規ページ・新規 CSS を足すときも、本文段落には同じ字下げを付ける  
- 対象外: 囲み（callout）、手順の注釈、表、キャプション、箇条書き、章扉・表紙など本文以外  
- **英語版**は字下げを付けない（`text-indent: 0` で版 CSS を分けてよい）  

## 節見出し（h3）は1ページに1つ（必須）

`h3.sec-title`（節見出し）は **1つの `sheet-wrap` / 1ページあたり最大1つ**。同じページに2つ以上載せない。必ずページ送りする。

- 節が続くときは `sheet-wrap` を分割する（例: `ch1-body-3` と `ch1-body-3b`）  
- `h2.page-title`（ページ大見出し）と `h3` の併用は可（キーワード早見・まとめなど）  
- 章扉・表紙・目次など `h3.sec-title` を使わないページ型はこのルールの対象外  

## 章末まとめの見出し

章末のまとめページ（`page-summary`）の見出しは **「この章のまとめ」** に統一する。  
「この章でできたこと」は使わない。  
まとめの箇条書きは **チェックリスト**（CSS で ☑）。通常の本文リスト（・）とは分ける。  
英語版は **“Chapter checklist”** などでよい（同じ `page-summary` クラス）。

## ことば・トーン

| 方針 | 内容 |
| --- | --- |
| 読者 | 非エンジニア寄りなら、画面操作・たとえ優先。実装詳細は囲みか付録へ |
| 分かりやすい言い換え | 抽象語より **場所・アプリ・道具** など具体語 |
| はじめに | 一人称の体験談トーン可 |
| 章本文 | 用語の地図と操作の順。はじめにの感想と役割分担する |
| 用語の初出 | 画面どおりの製品名。初出で役割を一文で補う |
| 英語 | 翻訳調・AI スロップを避け、自然な英語。UI スクショも英語 UI で撮り直す |

## キーワード早見・用語表

- 章の途中に大きな用語表を挟まず、**章の後半〜末尾**（まとめの直前が望ましい）に置く  
- 厳密定義より「たとえ」1行。あとから見返す用と明記してよい  
- 付録の用語集と食い違うときは、同じターンで両方を揃える  

## 図・対比図

| 項目 | ルール |
| --- | --- |
| 置き場 | 版の `images/`（例: `book/images/` または `book/JP/images/`）。HTML は相対パス |
| 形式 | 編集用 SVG ＋ 埋め込み用 PNG（印刷向けに **2x 以上**） |
| キャプション | `図: …` / EN は `Figure: …`。図内タイトルと HTML キャプションの二重掲載は避ける |
| 対比図 | 左右（または列）で **1行1軸**。ラベルは中央スパイン化してよい |
| 印刷 | 細いゼブラだけに頼らない。区切り線・帯・枠でコントラストを確保する |
| 差し替え後 | `src` が同じならファイル上書きのみ。ブラウザは「更新」で位置保持再読込 |

詳細パレット: `.claude/skills/kindle-book/references/diagrams.md`  
図の生成: `fireworks-tech-graph` 可。判断が難しければユーザー了承のうえ Fable レビュー可。

### 図解の色（必須）

概念図・対比図・関係図は **色を増やさない**。アクセントはポイント囲みと同じ緑だけ。

| 用途 | 色 |
| --- | --- |
| アクセント（主役） | **`#2f9e5f`**（`.callout-point` と同色） |
| 淡い緑の地 | `#f0faf4` / `#f7fcf9` |
| 既存・副次 | グレー `#6e6a64` / 枠 `#c8c4bc` / 線 `#8a8578` |
| 紙背景 | `#FFFEFA` |
| 本文・タイトル | 墨 `#3A3630` / 補足 `#8A8578` |

**やらない:** オレンジ・ブルーなど多色の役割分け。製品 UI スクショの再着色。

### 図の作成後チェック（必須）

SVG/PNG を書き出したあと、**ユーザーに渡す前に** 次を確認する。問題があれば直してから報告する。

1. **文字の重なり**  
2. **線と文字**（矢印が文字を貫いていない）  
3. **はみ出し**  
4. **余白**  
5. **コントラスト**  
6. **文言**（誤字・途中切れ・重複）  
7. **目視** — 書き出した **PNG を画像として開く**。座標計算だけで「大丈夫」としない  
8. **色トーン** — 緑＋グレー基調か  

直したら SVG と PNG を両方更新する。

## 画面キャプチャ（UI スクショ）（必須）

| 項目 | ルール |
| --- | --- |
| テーマ | **Light モード**で撮る（ユーザーがダーク指定したときだけ例外） |
| 切り出し | **説明に必要な領域だけ**。巨大な余白のフル画面縮小はしない |
| 解像度 | **deviceScaleFactor 2** 以上の PNG |
| 目視 | 差し込み前に **文字が読めるか・端が切れていないか・Light か** |
| 本文との一致 | 実 UI の遷移と食い違わない。仕様変更時は図と本文を同じターンで直す |
| 秘密情報 | API キー・パスワードが平文で写っていないか |

詳細: `.claude/skills/kindle-book/references/screenshots.md`

## 手順・初回フローの書き方

- **初めて触る読者**を本線にする（既存ユーザー前提で始めない）  
- 画面説明は **実際に触る順**  
- 深い設定は、必要な実践章の直前に回してよい  
- OS 別手順（Mac / Win）は **同じページに混ぜない**。分岐したらページを分ける  

## 執筆注記・脚注

- 執筆時点・Early Access などの注記は **ページ下部**（例: `.preface-footnote`）  
- 本文段落の末尾に長い ※ 注を直書きしない  
- **「続きは次のページです」など、紙面めくりを促す文言は書かない。** 手順がページをまたぐときは文を自然に切って続ける（`p.next-hint` の章末導線は可）  

## ポイント囲み（ラフ・行動喚起）

読者に「次は手を動かす」などを促す短い注釈は **`.callout-point`**。

| 項目 | 内容 |
| --- | --- |
| 見た目 | 緑の点線枠・白背景・ややいびつな角丸。左に指の線画 |
| 見出し | キッカー「ポイント」（`.callout-point-kicker`）／EN は “Tip” 可 |
| 用途 | 実践へつなぐ・励まし。長い解説は本文や通常 callout |
| 字下げ | 囲み内は **字下げしない** |
| 実装 | `with-text.css` の `.callout-point*`。消さない |

通常の囲み（`.callout-text` ＋ icon-a/b/c）とは別物。

## プレビュー用ツールバー

`with-text.html` のツールバー（`no-print`）:

- **更新**: スクロール位置を保ったまま再読み込み  
- **印刷 / PDF**: 印刷ダイアログ  

新規ページを足しても、この2ボタンと位置復元スクリプトは維持する。

## 出版用ドキュメント（任意）

| パス | 用途 |
| --- | --- |
| `docs/index.html` | ひな形の使い方（GitHub Pages 等） |
| `docs/JP/` | 日本語版: KDP・表紙依頼・Imagine プロンプト |
| `docs/EN/` | English: KDP (Amazon.com), cover brief, Imagine prompts |

中身のひな形は `docs/JP|EN/` を参照。書名・サブ・著者は各書籍で差し替える。

## 成果物の場所

```
book/                    # または book/JP/ と book/EN/
├── TASKS.md
├── with-text.html
├── templates.css
├── with-text.css
└── images/
docs/
├── index.html
├── JP/                  # 任意
└── EN/                  # 任意
.claude/skills/kindle-book/
```

詳細な制作手順は `.claude/skills/kindle-book/SKILL.md` を参照する。
